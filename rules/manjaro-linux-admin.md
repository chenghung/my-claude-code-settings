# Instruction

use the `manjaro-linux-admin` subagent for Manjaro or Arch Linux system administration, including diagnostics, log analysis, package management via pacman/yay/flatpak, and any operation requiring sudo or modifying system state.
